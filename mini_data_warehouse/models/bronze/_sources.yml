version: 2

sources:
  - name: bronze
    description: "This is the source schema for raw data ingested into the Bronze layer."
    database: "{{ 'bronze_prod' if target.name == 'prod' else 'bronze_dev' }}"
    schema: SEEDS
    tables:
      - name: seed__fund_data
      - name: seed__company_data
