version: 2

models:  
  - name: dim_company
    description: Company dimension (conformed attributes) with '_UNKNOWN' sentinel row.
    
    columns:
      - name: company_key
        tests: [not_null, unique]
      - name: company_id
        tests: [not_null, unique]