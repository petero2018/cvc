version: 2

models:  
  - name: dim_company
    description: Company dimension (conformed attributes) with '_UNKNOWN' sentinel row.
    
    columns:
      - name: company_key
        tests:
          - not_null:
              config:
                severity: warn
          - unique:
              config:
                severity: warn
      - name: company_id
        tests:
          - not_null:
              config:
                severity: warn
          - unique:
              config:
                severity: warn
